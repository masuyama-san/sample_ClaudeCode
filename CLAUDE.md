# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## プロジェクト概要

sample_ClaudeCode — 新規プロジェクト。

## 言語ルール

- すべてのアウトプットは日本語で記述する（コミットメッセージ、コードコメント、チャットの回答、PRの説明文など）
- プログラミングの識別子（変数名、関数名、クラス名、ファイル名、ディレクトリ名）は英語の適切な技術用語を使用する
- コード内のコメントも日本語で記述する

## セキュリティ・機密情報

- `.env`、`.secret`、credentials、APIキーなどの機密ファイルには絶対にアクセス・読み取り・変更しない
- 機密情報をコードやコミットに含めない
- 機密ファイルが`.gitignore`に含まれているか常に確認する

## 自己改善ルール

- 作業完了後、変更内容に対してビルド・テスト・リントを実行し、エラーがあれば自力で修正する
- 同じ間違いを繰り返した場合、このCLAUDE.mdに再発防止ルールを追記する
- 新しいビルドコマンドやテストコマンドが追加されたら、下記の「コマンド」セクションを更新する
- プロジェクト構成が大きく変わったら、このファイルの関連セクションを更新する

## 最適化ルール

- 不要なファイルの生成を避け、既存ファイルの編集を優先する
- 過剰な抽象化をしない。現在の要件に対して最小限の複雑さで実装する
- 変更に無関係なコードのリファクタリングやドキュメント追加をしない
- 並列実行可能なツール呼び出しは並列で行い、応答速度を最適化する
- 大きなタスクは計画モードで方針を整理してから実装に入る

## コマンド

<!-- プロジェクトにビルドツールやテストが追加されたら、ここにコマンドを記載する -->
<!-- 例: -->
<!-- - ビルド: `npm run build` -->
<!-- - テスト: `npm test` -->
<!-- - 単一テスト: `npm test -- --grep "テスト名"` -->
<!-- - リント: `npm run lint` -->
